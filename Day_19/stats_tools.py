#Task 2 — Statistics Module
#Create stats_tools.py with:
#highest(numbers)
#lowest(numbers)
#Import and test from main.py.
def highest(numbers):
    return max(numbers)
def lowest(numbers):
    return min(numbers)